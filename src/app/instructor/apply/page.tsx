import InstructorApplyClient from "./InstructorApplyClient";

export const metadata = {
  title: "Apply to Teach - SteppersLife",
  description: "Apply to become a dance instructor on SteppersLife and share your expertise with the stepping community.",
};

export default function InstructorApplyPage() {
  return <InstructorApplyClient />;
}
